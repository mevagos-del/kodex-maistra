import type { OfficialClassEntry } from '../types';
import { barbarian } from './barbarian';
import { bard, cleric, druid, monk, paladin, ranger, rogue, sorcerer, warlock } from './complete2024';
import { fighter } from './fighter';
import { wizard } from './wizard';

export const officialClasses: OfficialClassEntry[] = [
  barbarian,
  bard,
  cleric,
  druid,
  fighter,
  monk,
  paladin,
  ranger,
  rogue,
  sorcerer,
  warlock,
  wizard,
];
